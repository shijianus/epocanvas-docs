<div class="lg:sl-flex two-column-layout">
	{
		Astro.locals.starlightRoute.toc && (
			<aside class="right-sidebar-container print:hidden">
				<div class="right-sidebar">
					<slot name="right-sidebar" />
				</div>
			</aside>
		)
	}
	<div class="main-pane">
		<slot />
	</div>
</div>

<style>
	.two-column-layout {
		width: 100%;
	}

	.main-pane {
		isolation: isolate;
		min-width: 0;
		width: 100%;
	}

	@media (min-width: 72rem) {
		.right-sidebar-container {
			order: 2;
			position: relative;
			width: calc(
				var(--sl-sidebar-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-width)) / 2
			);
		}

		.right-sidebar {
			position: fixed;
			top: 0;
			border-inline-start: 1px solid var(--sl-color-hairline, #e2e8f0);
			padding-top: calc(var(--sl-nav-height) + 1rem);
			padding-inline-start: 1.5rem;
			padding-inline-end: 1rem;
			width: 100%;
			height: 100vh;
			overflow-y: auto;
			scrollbar-width: thin;
			scrollbar-color: var(--sl-color-hairline) transparent;
		}

		:global([data-has-sidebar][data-has-toc]) .main-pane {
			--sl-content-margin-inline: auto 0;
			order: 1;
			width: calc(
				var(--sl-content-width) + (100% - var(--sl-content-width) - var(--sl-sidebar-width)) / 2
			);
		}
	}
</style>
