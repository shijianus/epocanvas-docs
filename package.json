{
  "name": "epocanvas-docs",
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "prebuild": "node scripts/invalidate-render-cache.mjs",
    "build": "astro build",
    "deploy": "pnpm run build && wrangler pages deploy dist --project-name epocanvas-docs --branch main",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.32.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@pagefind/default-ui": "^1.5.2",
    "typescript": "^5.7.3",
    "wrangler": "^4.131.0"
  }
}
